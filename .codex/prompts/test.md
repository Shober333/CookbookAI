# Run Test Suite

Run the configured test suite and report results.

1. Read `CLAUDE.md` for current commands.
2. If stack commands are still TBD, say that clearly.
3. Run relevant unit, integration, lint, or typecheck commands when
   available.
4. Summarize pass/fail counts and key failures.
5. Suggest the smallest next fix when tests fail.

